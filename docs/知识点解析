//A = 找 IPv4
//AAAA = 找 IPv6
//MX = 找邮件服务器
//Upstream : 8.8.8.8:53：上游 DNS 服务器是 8.8.8.8，端口 53。
//Config file : dnsrelay.txt：当前使用的配置文件是 dnsrelay.txt。
//T03 .\dnsrelay.exe -d 8.8.8.8 dnsrelay.txt //程序启动后，先加载 dnsrelay.txt，再监听本地 53 端口；本地能命中的域名就直接处理，没命中的请求就转发给 8.8.8.8。
//T04 nslookup test1 127.0.0.1 //nslookup 把请求发给本机上的 dnsrelay，dnsrelay 再根据本地表或上游 DNS 返回结果
//T15 .\dnsrelay.exe -d 8.8.8.8 format-test.txt //-d 8.8.8.8：把上游 DNS 设为 8.8.8.8，并开启一级调试日志,format-test.txt：读取这个测试配置文件
//你的程序参数解析有个小坑：它按顺序匹配，上游 DNS 参数先被吃掉，剩下的最后一个作为配置文件名。
//所以：
//dnsrelay.exe -d 202.106.0.20 my_table.txt

//解释：
//第一个非选项参数 202.106.0.20 → 当作上游 DNS
//第二个非选项参数 my_table.txt → 当作配置文件
//老师问： 你这个并发性能怎么样？
//你答： 目前用单线程 select 事件驱动，不忙等待。TID 表支持 256 个并发中继请求。实际测试中同时开 10 个窗口 nslookup 都能正确处理，不串包不卡死。如果需要更高并发，可以调大 TID 表到 4096 和 socket 缓冲区到 256KB，单线程模型不需要加锁，性能足够覆盖校园网一个实验室的并发量。
//单线程 select 事件驱动：一个主循环，收到的每个请求都是立刻处理完再处理下一个。因为 DNS 查询处理很快（查个表、发个包、几十微秒的事），一个线程完全够用，还不用操心多线程的那些锁和同步问题。CPU不会忙等待
//QTYPE 名称     含义        典型用途
//  1    A       IPv4       地址，访问网站时查这个
//  28   AAAA    IPv6       地址，访问支持 IPv6 的网站
//  15   MX      邮件交换器  发邮件时查目标邮件服务器
